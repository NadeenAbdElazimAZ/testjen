pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                git branch: 'main', url: 'https://github.com/NadeenAbdElazimAZ/testjen.git' 
                sh 'ls'  
            }
        }
    }
}
